import * as actionTypes from "./actionTypes"

const initialState: ObjectInfoState = {
    object:[
      {
        name: 'test',
        description: "test",
        size:'test',
        weight:0
      }
    ]
  }


  const reducer = ()=>{
}

  export default reducer
